// Write code to create a function that accepts an array of numbers and a target number
// if the target number is present in the array, return its index
// else return -1
// You may not use the `indexOf`, `lastIndexOf` or `includes` methods

var linearSearch = function(arr, target) {
  console.log(arr);
  console.log(target);
  // iterate through the array, looking for target
  // if target is included, get it's index and return it
  // if target is not included, return -1
};
